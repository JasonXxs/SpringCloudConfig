server:
  port: 29080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka/

#服务名称
spring:
  application:
    name: @pom.artifactId@

#路由网关
zuul:
  routes:
    api-a:
      path: /api-web/**
      serviceId: consumer-web
    api-b:
      path: /api-shop/**
      serviceId: consumer-shop
    
profile:test